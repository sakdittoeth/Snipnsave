<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only covers the window before the
         first frame. It exists so that flash matches the §6 paper token. -->
    <style name="Theme.Snips" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/paper</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
